{
  "private": true,
  "name": "ampda",
  "version": "0.1.0",
  "packageManager": "pnpm@11.23.0",
  "scripts": {
    "build": "pnpm -r --workspace-concurrency=1 build",
    "dev": "pnpm -r dev",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "format": "prettier . --write",
    "verify": "node -e \"console.log('Repository OK')\""
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "prettier": "^3.9.6"
  }
}
